apiVersion: v1
kind: Pod
metadata:
  name: test-release-wiki-chart-test-connection
  labels:
    app: wiki
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['wiki:80']
  restartPolicy: Never
